# Local test MySQL connection. Copy to .env and fill in real values.
# .env is ignored by Git and is read only by the one-command API + DB runner.
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=lead93_test
MYSQL_PWD=replace-with-test-password
MYSQL_DATABASE=iic_msg_center
